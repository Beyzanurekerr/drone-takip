# Drone Hedef Takip - calisma bagimliliklari
#
#   pip install -r requirements.txt
#
# Cekirdek takipci (takip/) klasik CV'dir (DCF+Kalman+ego-motion), derin
# ogrenme GEREKTIRMEZ. Ama DEMO modu (main.py --mod demo) dedektor-oncullu
# calisir (A6, ROI dogrulama) - ultralytics/torch bu yuzden artik OPSIYONEL
# DEGIL, demo icin ZORUNLU (yalniz cekirdek takipciyi baska bir hatta
# GOMMEK isteyenler icin opsiyonel kalir).
#
# Olculen ortam: Python 3.10.12 - opencv-python 4.11.0 - numpy 1.26.4
# Alt sinirlar test edilmis surumlerdir; ust sinir yoktur.
# Ekransiz sunucuda opencv-python yerine opencv-python-headless de kullanilabilir
# (canli pencere calismaz, --penceresiz kosum ve tum olcumler calisir).

numpy>=1.24
opencv-python>=4.8

# --- demo modu + YOLO hedef secimi icin ZORUNLU ---
# Masaustu dogrulamasi icin CPU tekerlegi yeterlidir:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install ultralytics
# Agirliklar SESSIZCE INDIRILMEZ, elle yerlestirilir (bkz. README "Kurulum"):
#   weights/yolov8n.pt      (COCO taban, --yolo icin)
#   weights/a6_kucuk_hedef.pt (A6 UAVDT->VisDrone, demo modu icin)
# weights/ .gitignore'dadir.
ultralytics>=8.3
torch>=2.2
