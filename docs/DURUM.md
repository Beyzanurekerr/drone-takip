# Durum — demo-canli

Süreklilik dosyası: her alt-adım commit+push sonrası, her DUR'da güncellenir.

**Worktree / dal:** `~/dt_canli`, dal `demo-canli` (main'den `96d9c87`'de
ayrıldı; main'e D1/D2 ayrı sürüyor, buraya DOKUNULMAYACAK — merge kullanıcı
tarafından tetiklenecek).

**Son commit:** `96d9c87` (worktree kurulumu, henüz kod eklenmedi).

**Yapılan:** worktree + dal oluşturuldu, bu dosya yazıldı.

**Sıradaki adım:** `veri/gazebo_canli.py` (GazeboCanliKaynak + canli_senaryo +
klavye→cmd_vel + `--gui` desteği + `sure_sn` sınırlı-koşum desteği).

**Açık soru:** yok.

**Çalışan süreçler:** yok.
